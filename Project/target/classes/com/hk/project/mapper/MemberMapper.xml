<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hk.project.mapper.MemberMapper">

<select id="memberList" resultType="com.hk.project.vo.Member">
select * from members
</select>

<insert id="memberRegister">
INSERT INTO MEMBERS (MNO ,EMAIL ,PWD ,MNAME ,CRE_DATE ,MOD_DATE )
VALUES (MNO_SQE.NEXTVAL, #{email}, #{pwd}, #{mname}, sysdate, sysdate)
</insert>

<select id="memberUpdate" resultType="com.hk.project.vo.Member">
select * from members where mno=#{mno}
</select>

<update id="memberUpdateDone">
update MEMBERS set mname=#{mname}, email=#{email}, pwd=#{pwd} where mno=#{mno}
</update>

<delete id="memberDelete">
delete from members where mno=#{mno}
</delete>
</mapper>